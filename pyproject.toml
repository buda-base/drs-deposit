[project]
name = "drs-deposit"
version = "2.0.1"
description = "DRS Deposit Services for DRS 3"
readme = "README.rst"
requires-python = ">=3.13"
dependencies = [
    "bdrc-db-models>=2.0.3",
    "cryptography>=47.0.0",
    "pathlib>=1.0.1",
    "pillow>=12.2.0",
    "s3pathlib>=2.3.6",
    "tqdm>=4.67.3",
]

# uv native group
[dependency-groups]
dev =  [
    "apache-airflow>=2.0.0",
    "sphinx",
    "docutils",  # reST processing
    "pip>=26.1.1",
    "debugpy>=1.8.20",
    # add other dev-only tools here
]
